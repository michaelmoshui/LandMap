# Development & test dependencies (pinned)
-r requirements.txt
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
ruff==0.8.4
# Ingest-only: robust polygon geometry ops (union/difference/validity) used by
# app.ingest.boundaries. Not a runtime dependency - the API never imports it.
shapely==2.0.6
